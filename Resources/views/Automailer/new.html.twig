<h1>Automailer creation</h1>

{{ form_start(form, {action: path('automailer_create')}) }}
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
{{ form_end(form) }}

<ul class="record_actions">
    <li>
        <a href="{{ path('automailer') }}">
            Back to the list
        </a>
    </li>
</ul>
